To run the python file you will have to install the following dependencies
1. pandas
2. numpy
3. matplotlib

You can install the dependencies by typing following commands on the terminal
1. pip install pandas
2. pip install numpy
3. pip install matplotlib